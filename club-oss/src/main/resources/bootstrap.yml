spring:
  application:
    name: club-oss
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        server-addr: 8.152.209.169:8848
        prefix: ${spring.application.name}
        group: DEFAULT_GROUP
        namespace:
        file-extension: yaml
      discovery:
        server-addr: 8.152.209.169:8848
        enabled: true


